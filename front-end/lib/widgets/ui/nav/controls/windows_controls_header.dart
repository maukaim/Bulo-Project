import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

class WindowsControlsHeader extends ConsumerWidget {
  const WindowsControlsHeader({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    //TODO-ANTOINE: You can handle here the integration with the Windows Control buttons
    return Container(
      color: Colors.orangeAccent,
    );
  }
}
